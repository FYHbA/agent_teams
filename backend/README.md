# agents_team backend

FastAPI service for:

- project discovery
- Codex integration scaffolding
- project runtime initialization
- workflow planning
- workflow run persistence
- local filesystem browsing

Run with:

```bash
uvicorn app.main:app --reload --app-dir backend
```
